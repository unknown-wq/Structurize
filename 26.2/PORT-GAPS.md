# PORT-GAPS — что отключено, деградировано или не проверено

Рабочий список на «потом». Заполняется оркестратором по отчётам агентов, по строке на каждый срез.
Правило заказчика: **зелёная сборка важнее полноты фич, но всё вырезанное должно быть
записано так, чтобы это можно было починить, не занимаясь археологией.**

Каждой строке здесь обязан соответствовать маркер в коде:
```java
// TODO(port-26.2): DISABLED — <причина одной строкой>
/* … оригинальный код нетронутым … */
```
Сверка перед сдачей: `grep -rn "TODO(port-26.2)" src | wc -l` == сумме колонки «маркеров».

Колонки:
- **Что видно в игре** — наблюдаемое следствие. Если следствия нет, так и писать («невидимо»).
- **Как чинить** — конкретная зацепка: какой класс, какой API 26.2, что проверить.
- **Приоритет** — 🔴 серверный геймплей · 🟡 клиентская визуалка · 🟢 совместимость/косметика.

**Текущий счёт маркеров: 19** (`grep -rn "TODO(port-26.2)" src | wc -l` — сверено 31.07.2026, после фазы 1).

---

## Отключённый контент

| # | Файл:строка | Маркеров | Что отключено | Почему | Что видно в игре | Как чинить | Приоритет |
|---|---|---:|---|---|---|---|---|
| 1 | `client/gui/GuiStubs.java:48,59,71,88,100,119,131,142,153`; исключены из sourceSet `client/gui/{Window*,Abstract*}.java` и `client/gui/util/{InputFilters,ItemUtil}.java` — 13 файлов, 4496 строк | 9 | Весь графический интерфейс мода: инструмент постройки, сканер, выбор формы, менеджер паков, тег-тул, undo/redo, замена блоков, а также чтение состояния экрана | Мод строит GUI на `com.ldtteam.blockui` — отдельной библиотеке ldtteam, порт которой на 26.2 делает заказчик и который приедет позже. Реализовывать подмножество BlockUI внутри Structurize нельзя: MineColonies зависит от той же библиотеки и требует **34 различных символа** против наших 20 (замерено: 146 из 2061 java-файла MineColonies, 466 импортов, 94 XML-layout'а). Подмножество пришлось бы дорастить ровно до полной библиотеки | Ни одно окно не открывается. Инструменты (`ItemBuildTool`, `ItemScanTool`, `ItemShapeTool`, `ItemTagTool`) по клику не делают ничего. Серверная часть — постановка, операции, undo/redo, сеть — работает полностью | Фаза 4: убрать блок `sourceSets.main.java { exclude … }` из `build.gradle`, вернуть тела 9 методов `GuiStubs` из комментариев, портировать окна против живого API BlockUI. XML-layout'ы (`assets/structurize/gui/*.xml`, 454 строки) не тронуты и валидны | 🟡 |
| 2 | `items/ItemScanTool.java:264`, `items/ItemTagSubstitution.java:127` | 2 | `IItemExtension#getHighlightTip(ItemStack, Component)` | NeoForge-расширение, ванильного аналога в 26.2 нет | Имя предмета над хотбаром больше не дописывает « - \<активный слот\>» / « - \<поглощённый блок\>». Тот же текст остался в тултипе предмета | Миксин на рендер имени предмета в `Gui` | 🟢 |
| 3 | `items/ItemBuildTool.java:65`, `items/ItemShapeTool.java:49` | 2 | `IItemExtension#getCraftingRemainingItem` / `hasCraftingRemainingItem` | NeoForge-расширения; ванильный `Item.Properties#craftRemainder(Item)` не умеет «вернуть тот же самый стек» | Билд-тул и шейп-тул в крафте расходуются. В самом моде рецепта с ними нет — видно только датапакам | Миксин на `ItemStack#getCraftingRemainingItem` | 🟢 |
| 4 | `blockentities/BlockEntityTagSubstitution.java:245` | 1 | `BlockEntity#removeComponentsFromTag(CompoundTag)` | Метода в 26.2 нет (0 вхождений в `/opt/mc-src`) | Невидимо: ваниль сама вычищает component-ключи при сборке стека из блок-энтити | Проверить на живом сервере, что тег `captured_block` не дублируется в NBT предмета | 🟢 |

## Функциональная деградация

| # | Файл:строка | Маркеров | Что деградировало | Почему | Что видно в игре | Как чинить | Приоритет |
|---|---|---:|---|---|---|---|---|
| 1 | `compat/itemhandler/ItemHandlers.java:14`, `api/ItemStackUtils.java:106,206` | 3 | Поиск инвентарей через capability-API NeoForge (`Capabilities.ItemHandler.BLOCK/ENTITY/ENTITY_AUTOMATION`, обращение с посторонней `Direction`) — заменён на ванильный `Container` | На Fabric capability-API нет, а `fabric-transfer-api-v1` несовместим по семантике (`Storage<ItemVariant>` вместо слотов) | «Требуемые предметы» для отсканированного блока, чей инвентарь публикуется **только** модовой capability, выходят пустыми. Все ванильные контейнеры — сундуки, бочки, шалкеры, воронки, печи, рамки — считаются полностью | Либо мост на `ItemStorage.SIDED` из `fabric-transfer-api-v1` внутри `ItemHandlers`, либо оставить как есть: фасад `compat/itemhandler/IItemHandler` уже изолирует это в одном месте | 🟢 |
| 2 | `compat/common/config/Configurations.java:33` | 1 | Синхронизация серверного конфига на подключающегося клиента | На Fabric нет аналога `ModConfigSpec` типа `SERVER` с автосинхронизацией при логине | Клиент на удалённом сервере видит **свои локальные** значения `getServer()` (лимиты скана, дальность телепорта). Решения при этом принимаются на сервере и корректны — расходится только то, что клиент показывает | Добавить конфиг-пейлоад в `PlayMessageType` и слать при логине | 🟢 |
| 3 | `compat/common/fakelevel/FakeLevel.java:59` | 1 | `IFakeLevelLightProvider#getShade` — принудительно плоское затенение граней в превью | В 26.2 нет ни `Level#getShade(Direction, boolean)`, ни `BlockAndTintGetter`; затенение уехало в клиентский `BlockModelLighter` | Превью схематики затеняет грани по-ванильному, а не принудительно ровно. Заметно только при прямом сравнении | Смотреть `/opt/mc-src/net/minecraft/client/renderer/block/BlockModelLighter.java` | 🟡 |

## Не проверено (и почему)

| Область | Что именно не проверено | Причина |
|---|---|---|
| `client/**` целиком | Рендер превью схематики, оверлеи, шейдер, модели-оверлеи, кейбинды | В контейнере нет дисплея. `runServer` клиентский код не исполняет. Устанавливается только чистая компиляция. **Проверку рендера выполняет заказчик на живом клиенте (фаза 5) и присылает логи** |
| AccessWidener | Что все 8 перенесённых строк реально нужны и что ни одна не осталась от мёртвого кода | Loom валидирует существование члена, но не его использование |
| `compat/common/fakelevel/FakeLevel` | Что 29 реализованных абстрактных методов ведут себя как на NeoForge при живом рендере | Компиляция проходит; поведение видно только на клиенте |

## Расхождения датагена с оракулом

Оракул — JSON в `src/datagen/generated/structurize/`, скопированные из `1.21.1/`.

_(пусто — заполняется после первого `runDatagen`)_
