# PORT-GAPS — что отключено, деградировано или не проверено

Рабочий список на «потом». Заполняется оркестратором по отчётам агентов, по строке на каждый срез.
Правило заказчика: **зелёная сборка важнее полноты фич, но всё вырезанное должно быть
записано так, чтобы это можно было починить, не занимаясь археологией.**

Каждой строке здесь обязан соответствовать маркер в коде:
```java
// TODO(port-26.2): DISABLED — <причина одной строкой>
/* … оригинальный код нетронутым … */
```
Сверка перед сдачей: `grep -rn "TODO(port-26.2)" src | wc -l` == числу строк в таблицах ниже.

Колонки:
- **Что видно в игре** — наблюдаемое следствие. Если следствия нет, так и писать («невидимо»).
- **Как чинить** — конкретная зацепка: какой класс, какой API 26.2, что проверить.
- **Приоритет** — 🔴 серверный геймплей · 🟡 клиентская визуалка · 🟢 совместимость/косметика.

---

## Отключённый контент

| # | Файл:строка | Что отключено | Почему | Что видно в игре | Как чинить | Приоритет |
|---|---|---|---|---|---|---|
| 1 | `client/gui/*.java` (11 окон + `AbstractWindowSkeleton`), `client/gui/util/{InputFilters,ItemUtil}.java` — 13 файлов, 4496 строк, исключены из sourceSet в `build.gradle` | Весь графический интерфейс мода: инструмент постройки, сканер, выбор формы, менеджер паков, тег-тул, undo/redo, замена блоков | Мод строит GUI на `com.ldtteam.blockui` — отдельной библиотеке ldtteam, порт которой на 26.2 делает заказчик и который приедет позже. Реализовывать подмножество BlockUI внутри Structurize нельзя: MineColonies зависит от того же BlockUI и требует **34 различных символа** против наших 20 (замерено: 146 из 2061 java-файлов MineColonies, 466 импортов, 94 XML-layout'а). Подмножество пришлось бы дорастить ровно до полной библиотеки | Ни одно окно не открывается. Все предметы-инструменты (`ItemBuildTool`, `ItemScanTool`, `ItemShapeTool`, `ItemTagTool`) при клике не делают ничего. Серверная часть — постановка, операции, undo/redo по команде, сеть — работает полностью | Фаза 4: убрать три строки `exclude` из `build.gradle`, вернуть тела 9 методов `client/gui/GuiStubs.java` из комментариев, портировать окна против живого API BlockUI. XML-layout'ы (`assets/structurize/gui/*.xml`, 454 строки) не тронуты и валидны | 🟡 |

## Функциональная деградация

_(пусто — заполняется по ходу порта)_

## Не проверено (и почему)

| Область | Что именно не проверено | Причина |
|---|---|---|
| `client/**` целиком | Рендер превью схематики, оверлеи, шейдер, модели-оверлеи | В контейнере нет дисплея. `runServer` клиентский код не исполняет. Устанавливается только чистая компиляция. **Проверку рендера выполняет заказчик на живом клиенте (фаза 5) и присылает логи** |

## Расхождения датагена с оракулом

Оракул — JSON в `src/datagen/generated/structurize/`, скопированные из `1.21.1/`.

_(пусто — заполняется после первого `runDatagen`)_
